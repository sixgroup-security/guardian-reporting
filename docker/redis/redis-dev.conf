# No need for TLS in development
tls-port 0

# Bind to all IP addresses for local testing (be careful if exposing to the internet)
bind 0.0.0.0

# Disable password for development
requirepass ""

# Allow all clients without a certificate
tls-auth-clients no

# Disable protected mode for testing
protected-mode no
